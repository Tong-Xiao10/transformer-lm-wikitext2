# WikiText-2 Transformer

一个基于 PyTorch 的 Transformer 语言模型训练框架，支持 WikiText-2 数据集自动下载、训练、验证、测试与可视化。

---

## 一键运行

```bash
git clone &lt;your-repo&gt;
cd LLMs_safety
pip install torch matplotlib numpy requests
python main.py